spring:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update   # 처음만 create도 OK
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace

