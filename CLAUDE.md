# Claude Code 프로젝트 지침

## 문서 목적

이 문서는 `ticketing-reservation-service`에서 Claude Code가 작업을 시작할 때 읽는 짧은 진입 문서다.
상세 기술 규칙이나 기존 설계 문서를 복사하지 않고, 역할·승인 게이트·참조 문서만 정의한다.

`CLAUDE.md` 200줄 이하는 이 프로젝트의 가독성 품질 목표이며 Claude Code의 기술적 로드 제한이 아니다.

## 프로젝트 목적

- Spring Boot 기반 티켓팅·좌석예약 백엔드 포트폴리오를 구현한다.
- 트랜잭션, 동시성, 데이터 정합성, Redis 활용, 장애 대응과 테스트 근거를 설명 가능한 상태로 만든다.
- 기존 코드와 저장소 문서를 우선 확인하고 확인되지 않은 상태를 추측하지 않는다.

## 역할

- Claude Code: 사용자가 승인한 범위의 작성·실행 담당
- GPT: 구현 결과를 별도로 검토하는 독립 검토자
- 사용자: 범위, 설계, 구현, Git 작업과 최종 진행 여부를 결정하는 승인자

GPT의 검토 결과와 프로젝트 Agent의 검토 결과는 권고와 검증 자료다. 최종 승인 권한은 사용자에게 있다.

## 작업 시작 규칙

1. 현재 TASK, GitHub Issue, 승인된 단계와 허용 범위를 확인한다.
2. 기존 코드와 관련 기준 문서를 먼저 읽는다.
3. 필요한 입력 자료가 부족하면 구현하지 않고 부족한 자료를 보고한다.
4. 구현 전 설계 승인이 필요한 단계에서는 사용자의 승인을 받은 뒤 작업한다.
5. 현재 단계에서 허용된 파일과 명령만 사용한다.

## 승인 게이트

- 사용자가 승인하기 전에는 구현을 시작하지 않는다.
- 브랜치 생성·전환, 커밋, push, PR 생성, merge는 각각 승인된 단계에서만 실행한다.
- 테스트를 실행하지 않았다면 실행하거나 통과했다고 표현하지 않는다.
- 다음 단계나 다음 TASK를 자동으로 진행하지 않는다.

## 범위 통제

- TASK 범위 밖 파일이나 기능을 임의로 변경하지 않는다.
- 범위 밖 문제는 구현하지 않고 `[범위 밖 제안]`으로 분리해 보고한다.
- 기존 문서가 중복되거나 충돌하면 임의로 하나를 선택하거나 통합하지 않는다.
- 확인되지 않은 프로젝트 상태, 버전, 규칙과 테스트 결과를 사실처럼 작성하지 않는다.
- 오류가 발생하면 우회하지 않고 실행한 명령과 오류를 보고한다.

## 검증과 보고

- 변경 전후 Git 상태를 확인한다.
- 실행한 명령, 실제 출력, 변경 파일과 미확인 사항을 구분해 보고한다.
- 민감정보와 사용자 로컬 절대 경로는 저장소 문서에 기록하지 않는다.
- 코드 위험 검토가 필요하면 프로젝트 Agent와 GPT 독립 리뷰 계약을 따른다.

## 기준 문서

작업 목적에 맞춰 다음 문서를 확인한다.

- 교차 작업 규칙: `docs/process/PROJECT-RULES.md`
- TASK 상태와 순서: `docs/process/PORTFOLIO-ROADMAP.md`
- TASK 시작 전 자료: `docs/process/TASK-START-CHECKLIST.md`
- GPT 독립 검토 계약: `docs/process/GPT-REVIEW-CONTRACT.md`

상세 프로젝트 설명과 기술 설계는 위 문서가 안내하는 기존 저장소 문서를 참조한다.
