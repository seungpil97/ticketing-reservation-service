# 3주차 Wrap-up (2026-03-14)

## 이번주 한 일
- HOLD 만료 해제 스케줄링 구현
  - 만료된 HOLD를 주기적으로 EXPIRED 처리
  - 좌석 상태 HELD → AVAILABLE 복구
- 예약 확정(RESERVE) API 구현
  - `POST /holds/{holdId}/reserve`
  - HOLD → RESERVE 상태 전이
- 개발환경 안정화
  - docker-compose dev/test DB 포트 분리 (13305 / 13307)
  - direnv 도입으로 환경변수 자동 로드
  - ci.yml 환경변수 중복 제거

## 기술 결정
- HoldStatus에 CONFIRMED 추가: 만료된 선점(EXPIRED)과 예약 확정된 선점(CONFIRMED)을 명확히 구분하기 위해
- 스케줄러 주기 30초: 만료 즉시 처리보다 주기적 배치 처리가 DB 부하 측면에서 효율적이기 때문에
- docker-compose 포트 분리: dev/test가 같은 DB를 공유하면 테스트 데이터가 개발 환경에 영향을 줄 수 있으므로

## 트러블슈팅
- `markAvailable()` 조건 문제: HOLD 만료 시 HELD → AVAILABLE 복구 로직에 조건이 있어 예약 취소 시 RESERVED 상태에서 복구가 안 되는 버그 발견
  - 조건 제거 후 상태에 무관하게 AVAILABLE로 복구되도록 수정
- application-test.yml 포트 불일치: 기본값이 13306이었으나 docker-compose 변경 후 13307로 수정

## 다음주 계획
- 동시성 제어 (비관적 락)
- N+1 문제 해결 및 인덱스 설계
- 예약 취소 API 구현
- Redis 분산락 도입